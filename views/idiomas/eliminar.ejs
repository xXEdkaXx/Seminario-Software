<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Eliminar Idioma</title>
    <style>
        body {
            background-color: #121212;
            color: #ffffff;
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 0;
            padding: 20px;
        }
        h1 {
            color: #007BFF;
        }
        form {
            background-color: #1e1e1e;
            padding: 20px;
            border-radius: 10px;
            display: inline-block;
            text-align: left;
        }
        label {
            display: block;
            margin: 10px 0 5px;
        }
        input {
            background-color: #1e1e1e;
            color: white;
            border: 1px solid #444;
            padding: 5px;
            border-radius: 5px;
            width: 100%;
        }
        .button-container {
            display: flex;
            gap: 10px;
            margin-top: 10px;
        }
        button {
            background-color: #ff4081;
            color: white;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 5px;
            width: 100%;
            flex: 1;
        }
        button:hover {
            background-color: #d83670;
        }
        .cancel-button {
            background-color: #444;
        }
        .cancel-button:hover {
            background-color: #333;
        }
    </style>
</head>

<body>
    <h1>Eliminar Idioma para el empleado: <%= empleado.nombre %> <%= empleado.apellido %></h1>
    <div class="container">
        <form action="/deleteIdiomas" method="POST">
            <label for="codigo">Código</label>
            <input type="text" name="codigoI" id="codigoI" value="<%= idiomas.codigoI %>" readonly>
            <br><br>
            <label for="nombreI">Idioma:</label>
            <input type="text" name="nombreI" class="nombreI" value="<%= idiomas.nombreI %>" readonly>
            <br><br>
            <label for="tiempoI">Tiempo Hablando idioma:</label>
            <input type="text" name="tiempoI" id="tiempoI" value="<%= idiomas.tiempoI %>" readonly>
            <br><br>
            <div class="button-container">
                <button type="submit">Eliminar</button>
                <a href="/Empleados2" style="flex: 1;"><button type="button" class="cancel-button">Cancelar</button></a>
            </div>
        </form>
    </div>
</body>

</html>